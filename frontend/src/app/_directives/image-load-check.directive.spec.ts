import { ImageLoadCheckDirective } from './image-load-check.directive';

describe('ImageLoadCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadCheckDirective();
    expect(directive).toBeTruthy();
  });
});
