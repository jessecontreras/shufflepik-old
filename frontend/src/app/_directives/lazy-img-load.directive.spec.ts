import { LazyImgLoadDirective } from './lazy-img-load.directive';

describe('LazyImgLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImgLoadDirective();
    expect(directive).toBeTruthy();
  });
});
