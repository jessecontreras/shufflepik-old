module.exports = function (site) {
    var hash = {
        twitter: require('./sites/twitter'),
        facebook: require('./sites/facebook'),
        vk: require('./sites/vk')
    };

    return hash[site.toLowerCase()];
};
